/*
 * raygun
 * https://github.com/MindscapeHQ/raygun4node
 *
 * Copyright (c) 2015 MindscapeHQ
 * Licensed under the MIT license.
 */
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var child_process_1 = require("child_process");
var debug = require("debug")("raygun");
var requestProcessSource = require.resolve("./raygun.sync.worker");
function syncRequest(httpOptions) {
    var requestProcess = child_process_1.spawnSync("node", [requestProcessSource], {
        input: JSON.stringify(httpOptions),
    });
    console.log(requestProcess.stdout.toString());
}
function send(options) {
    try {
        var request = syncRequest(options);
    }
    catch (e) {
        console.log("Raygun: error " + e + " occurred while attempting to send error with message: " + options.message);
    }
}
exports.send = send;
